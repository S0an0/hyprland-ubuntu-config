#%PAM-1.0

# Keep retries responsive while retaining a small anti-bruteforce delay.
auth       optional     pam_faildelay.so delay=250000
auth       required     pam_unix.so try_first_pass nodelay

account    include      common-account
session    include      common-session
